<div class="main_header">
  <h1>Read With Me?</h1>
  <p> --- </p>
</div>

<div class="section_header">
<p> Hi, <%= @current_user.name %>. Here's what you've read:</p>
<%= render "filter" %>
  <% @books.each do |book| %>
  <div class="book" data-city="<%= book.city %>">
    <h3><%= book.title %></h3>
  </div>
<% end %>
